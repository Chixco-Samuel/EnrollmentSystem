package com.enrollment.core.validation;

import com.enrollment.domain.Schedule;

public class FacultyConflictValidator implements ScheduleValidator {
    @Override
    public void validate(Schedule newSchedule) {
       
    }
}
